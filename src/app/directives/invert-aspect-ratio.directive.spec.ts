import { InvertAspectRatioDirective } from './invert-aspect-ratio.directive';

describe('InvertAspectRatioDirective', () => {
  it('should create an instance', () => {
    const directive = new InvertAspectRatioDirective();
    expect(directive).toBeTruthy();
  });
});
